package co.edu.uniquindio.proyecto.Enum;

public enum PaymentState {
    COMPLETED,
    FAILED,
    CANCELED,
    PENDING,
    pending,
    approved,
    authorized,
    in_process,
    in_mediation,
    rejected,
    cancelled,
    refunded,
    charged_back
}
